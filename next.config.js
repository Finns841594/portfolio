/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'upload.wikimedia.org',
      `img.freepik.com`,
      `images.unsplash.com`,
      'github.com',
    ],
  },
}

module.exports = nextConfig
